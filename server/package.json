{
  "name": "yacd-server",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "description": "yacd Express 后端:静态托管 + API 代理到 Mihomo + 订阅管理",
  "scripts": {
    "start": "node index.js",
    "test": "node --test --test-concurrency=1"
  },
  "dependencies": {
    "express": "^5.2.1",
    "http-proxy-middleware": "^4.1.1",
    "js-yaml": "^5.2.0"
  },
  "devDependencies": {
    "supertest": "^7.0.0"
  }
}
